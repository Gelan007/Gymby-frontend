import React from 'react';
import s from "./Buttons.module.scss";

const ButtonRed = (props) => {
    return (
        <button className={s.buttonRed} {...props}>

        </button>
    );
};

export default ButtonRed;