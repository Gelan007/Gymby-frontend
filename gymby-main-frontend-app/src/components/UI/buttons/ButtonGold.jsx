import React from 'react';
import s from './Buttons.module.scss'

const ButtonGold = (props) => {
    return (
        <button className={s.buttonGold} {...props}>

        </button>
    );
};

export default ButtonGold;