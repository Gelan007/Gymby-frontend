import React from 'react';
import s from './Buttons.module.scss'

const ButtonOrange = (props) => {
    return (
        <button className={s.buttonOrange} {...props}>
            
        </button>
    );
};

export default ButtonOrange;